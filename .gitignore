# Go
/wirenest-panel
*.exe

# Node / frontend
web/node_modules/
web/dist/
web/tsconfig.tsbuildinfo

# Built frontend assets (regenerated by `make web`); keep the placeholder index.html
internal/web/dist/assets/
internal/web/dist/*.js
internal/web/dist/*.css

# Local state
*.db
*.sqlite

# Secrets / local credentials — never commit
.admin_pass
*.conf
secrets.json
credentials.json
settings.json

# Local tooling / scratch
.claude/settings.local.json
*.mjs

# Editor / OS
.DS_Store
*.swp
